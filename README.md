This task focuses on retrieving data from the Hospital database using SQL SELECT statements.
It includes basic queries, filtering conditions, sorting, and limiting results.

Contents

SELECT * and column-based queries

Filtering with WHERE, AND, OR, LIKE, BETWEEN

Sorting using ORDER BY

Limiting output using LIMIT
